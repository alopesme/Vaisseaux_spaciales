#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <math.h>
#include "Vitesse.h"
#include "Tires.h"
#include "Deplacement.h"
#include "Vaisseaux.h"
#include "Monde.h"
#include "Test.h"
#include <MLV/MLV_all.h>

int main(int argc, char const *argv[]) {
	/*int x, y;
	Coord_Tir coord_t;

	MLV_create_window("test", "test", 600, 600);

	MLV_wait_mouse(&x, &y);

	init_coord_tir(600 / 2, 600 - 20, &coord_t);

	while ((coord_t.tir_x < 600 || coord_t.tir_y < 600)
		|| (coord_t.tir_x > 0 || coord_t.tir_y > 0)) {
		MLV_clear_window(MLV_COLOR_BLACK);

		MLV_draw_circle(coord_t.tir_x, coord_t.tir_y, 5, MLV_COLOR_RED);
		calculer_tir(&coord_t, x, y);

		MLV_actualise_window();
	}

	MLV_free_window();*/



	return 0;
}
